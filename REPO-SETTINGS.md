# One-time GitHub settings (do these in the UI after pushing)

These live in repo settings, not in files — they're what makes the repo look
finished in lists, search, and link unfurls.

## About (right sidebar → gear icon)

**Description:**
> Claude Agent Skill that turns a chat into a grounded domain expert — sources over priors, with an eval-validated anti-prior rule (0/5 → 5/5).

**Website:** your portfolio or LinkedIn URL.

**Topics** (searchable; use 5–10):
`claude` `claude-skills` `agent-skills` `prompt-engineering` `llm` `ai-agents` `research-agent` `evals` `anthropic`

## Social preview (Settings → General → Social preview)

Upload `assets/social-preview.png` (already 1280×640, <1MB — GitHub's recommended spec).
This is what shows when the repo is linked on LinkedIn/X.

## Profile

Pin this repo on your profile (recruiters weight pinned repos heavily), and make
sure your profile has your location + LinkedIn link so you're findable.

## Optional next-level

- Publish the `faithfulness-suite` eval harness repo and link it from the Evals
  section — shipped evals are the rarest credibility signal in the skill ecosystem.
- A short demo GIF of an actual run (VHS/asciinema, or a cropped screen capture of
  the readout) placed under "How it works".

*(Delete this file once done — it's a checklist, not repo content.)*
